# Admin SPA env vars are read from the repo root (.env / .env.local).
# See ../../.env.example for the full list (VITE_REPO_*, VITE_FIREBASE_*, etc.).
#
# Optional: put overrides here — they take precedence over the root file.
